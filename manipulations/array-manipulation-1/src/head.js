/* exported head */

// head(array) before:
// return array at index 0;

function head(array) {
  return array[0];
}

// head(array) after:
// define a function named head, which takes one parameter, an array
// return index 0 of the argument array
