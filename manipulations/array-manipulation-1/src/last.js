/* exported last */

// last(array) before:
// define a function named last which takes one parameter, an array
// define a variable named lastIndex, assign it the value of array.length-1
// return the value at array[lastIndex]

function last(array) {
  var lastIndex = array.length - 1;
  return array[lastIndex];
}

// no change
