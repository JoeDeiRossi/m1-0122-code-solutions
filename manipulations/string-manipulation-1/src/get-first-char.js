/* exported getFirstChar */

// getFirstChar before:
// define a function named getFirstchar that takes one parameter, a string
// return string[0], the first character of the string

function getFirstChar(string) {
  return string[0];
}

// no change
