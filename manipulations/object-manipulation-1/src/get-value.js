/* exported getValue */

// getValue(object, key) before:
// define a function getValue that takes 2 parameters, an object and a key in the form of a string
// use dot notation to return the value stored at the given key in object

function getValue(object, key) {
  return object[key];
}

// getValue(object, key) after:
// define a function getValue that takes 2 parameters, an object and a key in the form of a string
// use BRACKET notation to return the value stored at the given key in object
